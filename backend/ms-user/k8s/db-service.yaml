apiVersion: v1
kind: Service
metadata:
  name: user-db
  namespace: smartlogix
  labels:
    app: user-db
    service: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: user-db