apiVersion: v1
kind: Service
metadata:
  name: bff
  namespace: smartlogix
  labels:
    app: bff
    service: backend
spec:
  type: ClusterIP
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  selector:
    app: bff