apiVersion: v1
kind: Service
metadata:
  name: orders-db
  namespace: smartlogix
  labels:
    app: orders-db
    service: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: orders-db