apiVersion: v1
kind: Service
metadata:
  name: orders-db
spec:
  selector:
    app: orders-db
  ports:
    - port: 5432
      targetPort: 5432

---
apiVersion: v1
kind: Service
metadata:
  name: ms-orders
spec:
  selector:
    app: ms-orders
  ports:
    - port: 9092
      targetPort: 9092
  type: ClusterIP