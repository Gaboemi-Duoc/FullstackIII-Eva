apiVersion: v1
kind: Service
metadata:
  name: ms-restock
  namespace: smartlogix
  labels:
    app: ms-restock
    service: backend
spec:
  type: ClusterIP
  ports:
  - port: 9093
    targetPort: 9093
    protocol: TCP
    name: http
  selector:
    app: ms-restock