apiVersion: v1
kind: Service
metadata:
  name: restock-db
  namespace: smartlogix
  labels:
    app: restock-db
    service: database
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
    name: postgres
  selector:
    app: restock-db